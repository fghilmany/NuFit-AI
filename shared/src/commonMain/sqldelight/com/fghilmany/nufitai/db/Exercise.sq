CREATE TABLE exercise (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    equipment_category TEXT NOT NULL,
    movement_pattern TEXT NOT NULL,
    level TEXT NOT NULL,
    level_variant INTEGER,
    level_note TEXT,
    flag_exclusion TEXT NOT NULL,
    flag_priority TEXT NOT NULL,
    loaded_body_areas TEXT,
    equivalent_substitutes TEXT,
    regression_chain TEXT,
    progression_chain TEXT,
    level_up_requirement TEXT,
    related_movement_patterns TEXT,
    high_impact INTEGER NOT NULL,
    isometric_heavy INTEGER NOT NULL,
    media_slug TEXT,
    primary_muscle_group TEXT NOT NULL,
    target_muscles_primary TEXT NOT NULL,
    target_muscles_secondary TEXT NOT NULL,
    instructions TEXT NOT NULL,
    common_mistakes TEXT NOT NULL,
    safety_tips TEXT NOT NULL
);

insertExercise:
INSERT INTO exercise(
    id, name, equipment_category, movement_pattern, level, level_variant, level_note,
    flag_exclusion, flag_priority, loaded_body_areas, equivalent_substitutes, regression_chain,
    progression_chain, level_up_requirement, related_movement_patterns, high_impact, isometric_heavy, media_slug,
    primary_muscle_group, target_muscles_primary, target_muscles_secondary, instructions,
    common_mistakes, safety_tips
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?);

getAllExercises:
SELECT * FROM exercise;

getExerciseById:
SELECT * FROM exercise WHERE id = ?;

countExercises:
SELECT COUNT(*) FROM exercise;
