package com.fghilmany.nufitai.domain.monthlyplan.entity

enum class PlanSource { LOCAL_TEMPLATE, LOGGED_IN_RULE_ENGINE }

enum class PlanStatus { ACTIVE, ARCHIVED }

enum class DayType { SESSION, REST, LIGHT_ACTIVITY }
